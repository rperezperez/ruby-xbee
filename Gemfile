#!/usr/bin/env bundle
# encoding: utf-8

source 'http://rubygems.org'

gemspec

group :assets do
  gem 'rake',               '~> 10.0.0'
  gem 'shoulda',            '~> 3.3.0'
  gem 'hanna-nouveau',      '~> 0.2.6', :require => false
end

group :test do
  gem 'simplecov',          '~> 0.7.1', :require => false
  gem 'minitest',           '~> 4.6.0', :require => false
  gem 'minitest-reporters', '>= 0.14.0', :require => false
end